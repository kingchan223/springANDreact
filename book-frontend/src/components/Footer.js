import React from 'react';
import styled from 'styled-components';

const StyledFooterDiv = styled.div`
  text-align: center;
  background-color: black;
  height: 100px;
`;

const Footer = () => {
  return <StyledFooterDiv></StyledFooterDiv>;
};

export default Footer;
